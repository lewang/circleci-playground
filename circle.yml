dependencies:
  override:
    - git clone https://github.com/theory/pgtap
    - cd pgtap
    - make
    - make installcheck
    - sudo make install
    - psql -c "CREATE EXTENSION pgtap;"

test:
  override:
    - exit 0
