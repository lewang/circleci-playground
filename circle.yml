machine:
  environment:
    cassandra-wait: "curl -sSL http://git.io/v0UAQ | sh"
dependencies:
  override:
    - exit 0

test:
  override:
    - sudo service cassandra start; $cassandra-wait; cqlsh -e "SHOW version"

