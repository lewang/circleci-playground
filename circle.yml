dependencies:
  override:
    - git clone https://github.com/theory/pgtap
    - make && make installcheck && sudo make install:
        pwd: pgtap
    - psql -c "CREATE EXTENSION pgtap;"

test:
  override:
    - exit 0
