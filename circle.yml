general:
  artifacts:
    # This doesn't work
    - "solo/target/**.tools.json"
   
    # But this works
    - "solo/target/1234.zip"

dependencies:
  override:
    - exit 0

test:
  override:
    - mkdir -p solo/target
    - touch solo/target/1234.tools.json
    - touch solo/target/1234.zip
