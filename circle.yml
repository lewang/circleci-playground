machine:
  environment:
    wait-for-cassandra: "curl -sSL http://git.io/v0UAQ | sh"
dependencies:
  override:
    - exit 0

test:
  override:
    - sudo service cassandra start; $wait-for-cassandra; cqlsh -e "SHOW version"

