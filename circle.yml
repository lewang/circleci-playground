general:
  artifacts:
    # This works
    - "solo/target/1234.zip"
   
    # But this doesn't work
    - "solo/target/*.tools.json"

    # Neither this one
    - "solo/target/**.tools.json"

dependencies:
  override:
    - exit 0

test:
  override:
    - mkdir -p solo/target
    - touch solo/target/1234.tools.json
    - touch solo/target/1234.zip

  post:
    - mkdir $CIRCLE_ARTIFACTS/json_output
    - cp solo/target/*.json $CIRCLE_ARTIFACTS/json_output
